---
description: Use the playwright-test-writing skill to write Playwright E2E tests.
---

This workflow invokes the `playwright-test-writing` skill to get detailed instructions for writing Playwright E2E tests using MCP browser for selector discovery and verification.

1.  **Invoke the skill**
    
    Use the `skill` tool with the following parameters:
    - `SkillName`: `playwright-test-writing`
