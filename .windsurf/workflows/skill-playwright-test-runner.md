---
description: Use the playwright-test-runner skill to run and debug Playwright tests.
---

This workflow invokes the `playwright-test-runner` skill to get detailed instructions for running and debugging Playwright E2E tests.

1.  **Invoke the skill**
    
    Use the `skill` tool with the following parameters:
    - `SkillName`: `playwright-test-runner`
